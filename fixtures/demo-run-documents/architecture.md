# Greeting Update Architecture

The change should stay inside the demo target repository.

- Update the greeting implementation in the existing source module.
- Update or add tests in the existing test suite.
- Avoid introducing new packages, services, or repository structure changes.
